name: 🏀 Scrape NBA Stats

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: 📦 Install dependencies
        run: npm install axios cheerio
        
      - name: 🕷️ Run scraper
        run: node scrape.js
        
      - name: 📤 Commit changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/
          git diff --staged --quiet || (git commit -m "📊 NBA Stats Update $(date -u +%Y-%m-%d\ %H:%M) UTC" && git push)
